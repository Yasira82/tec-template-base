name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

# One run per ref. Pushing again to a PR branch cancels the superseded run
# instead of racing it: the last commit is the only one anyone reads, and
# without this every intermediate push paid for a full pipeline — CI's
# job, a CodeQL analysis, a Playwright install-build-and-run — that was
# obsolete before it finished.
#
# `main` is EXCLUDED from cancellation on purpose: a run there is the
# post-merge record (and the status a deploy gate reads). Cancelling those
# would trade money for the one signal worth keeping.
#
# This expression is copied verbatim from `tec-knowledge-base`
# knowledge-ci.yml — the fleet had TWO forms of this rule and the other one
# (tec-app, `cancel-in-progress: true`) cancels main runs as well. One rule,
# one spelling (P2).
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ github.ref != 'refs/heads/main' }}


permissions:
  contents: read

jobs:
  # ── ONE job, ONE install ──────────────────────────────────────────────────
  #
  # This was five jobs: payment-policy · lint · typecheck · test · build. They
  # were correct and they were expensive, for two reasons that compound:
  #
  #   1. FOUR of them ran `npm install` — the same install, four times, on four
  #      runners, for one commit.
  #   2. GitHub bills each job ROUNDED UP TO THE MINUTE. `payment-policy` is a
  #      handful of greps that finish in seconds and cost a full minute anyway.
  #      Five jobs means five roundings before any real work is counted.
  #
  # Splitting them bought parallelism — a faster red light. On a platform built
  # by one person, wall-clock minutes are worth far less than the bill, and in
  # September 2026 the Actions budget reached 98% of its cap with `Stop usage`
  # armed, which would have frozen every open PR across 29 repos.
  #
  # Nothing is dropped. Every check that ran before still runs, in the order a
  # failure is cheapest to find: the greps need no install and go first; a type
  # error stops the run before the test suite and the build.
  #
  # e2e stays a separate workflow ON PURPOSE. It builds with different
  # `NEXT_PUBLIC_*` values (localhost gateway + E2E_MODE), and those are inlined
  # at BUILD time — so its build is a genuinely different artifact that this one
  # cannot stand in for. Merging them would mean two builds in one job, which
  # saves a runner and nothing else.
  verify:
    name: Verify
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      # No install needed — greps over src. First, so a policy violation costs
      # seconds instead of a full pipeline.
      - name: Payment contract policy (ADR-009) + CSRF placement
        run: |
          violations=0
          if grep -rniE 'x-service-secret|SERVICE_SECRET' src; then
            echo "::error::ADR-009: use x-internal-key / INTERNAL_SECRET (not x-service-secret / SERVICE_SECRET)"
            violations=1
          fi
          if grep -rnE 'amount:[[:space:]]*z\.string' src | grep -qi payment; then
            echo "::error::ADR-009: payment amount must be a number — no z.string() for amount in payment routes"
            violations=1
          fi
          if grep -rnE 'csrfCookie !== csrfHeader|csrfCookie === csrfHeader|CSRF validation failed|CSRF token mismatch' src/app/api 2>/dev/null; then
            echo "::error::CSRF must be enforced ONLY in middleware (P2). Remove route-level CSRF checks — they 403 legit Pi-Browser payments. (KB C-12 §11)"
            violations=1
          fi
          [ "$violations" -eq 0 ] && echo "payment contract policy OK" || exit 1

      - uses: actions/setup-node@v7
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install
        run: npm install --legacy-peer-deps

      - name: Typecheck
        run: npm run typecheck

      # Advisory, exactly as before: this step could never fail the build when it
      # was its own job (`continue-on-error: true`), and it still cannot. It now
      # costs the seconds it takes rather than a whole runner.
      - name: Lint
        run: npm run lint
        continue-on-error: true

      - name: Test
        run: npm test

      - name: Build
        run: npm run build
        env:
          NEXT_PUBLIC_PI_SANDBOX:      'true'
          NEXT_PUBLIC_API_GATEWAY_URL: ${{ secrets.NEXT_PUBLIC_API_GATEWAY_URL || 'https://api-gateway-production-6a68.up.railway.app' }}
          NEXT_PUBLIC_PI_APP_ID:       ${{ secrets.NEXT_PUBLIC_PI_APP_ID || 'test-app-id' }}
      # C-123 §11: a middleware Next.js does not load passes every unit test and
      # runs nowhere. The build's own manifest is the proof that it runs.
      - name: Middleware is compiled
        run: |
          node -e "const m=require('./.next/server/middleware-manifest.json').middleware; if(!m['/']){console.error('::error::middleware not compiled — it must live in src/ beside src/app (C-123 §11)');process.exit(1)} console.log('middleware compiled')"
